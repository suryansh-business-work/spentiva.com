/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS v4 uses CSS-first configuration
  // Custom colors are now defined in src/styles/tailwind.css using @theme directive
  // Content detection is automatic in v4
}
